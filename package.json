{
  "name": "chishenme",
  "version": "1.0.0",
  "description": "AI 情绪饮食助手 - 根据你的心情推荐美食",
  "main": "index.html",
  "scripts": {
    "start": "npx serve public",
    "build": "cp -r src/* public/ 2>/dev/null || true && cp index.html public/",
    "cap:init": "npx cap init 吃什么 com.chishenme.app --web-dir=public",
    "cap:add:ios": "npx cap add ios",
    "cap:copy": "npx cap copy ios",
    "cap:open": "npx cap open ios",
    "cap:sync": "npx cap sync ios"
  },
  "keywords": [
    "food",
    "emotion",
    "ai",
    "recommendation"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@capacitor/core": "^5.6.0",
    "@capacitor/ios": "^5.6.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.6.0",
    "serve": "^14.2.0"
  }
}
